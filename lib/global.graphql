subscription NotificationReceived($token: String!) {
  notificationReceived(token: $token) {
    message
  }
}

query Hello {
  hello {
    message
  }
}

query Authenticate {
  authenticate {
    message
  }
}

subscription Truths {
  truths
}
